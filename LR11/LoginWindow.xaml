<Window x:Class="LR11.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логін:" Margin="10"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="0" Margin="10" />

        <TextBlock Grid.Row="1" Text="Пароль:" Margin="10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="10" />

        <Button Grid.Row="2" Content="Увійти" Margin="10" Click="LoginButton_Click"/>
    </Grid>
</Window>
